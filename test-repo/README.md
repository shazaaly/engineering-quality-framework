# Test Repo

- Why: This is the demo target that proves governance behavior.
- How: Keep both compliant and violating code paths for live walkthrough.
- Example: `processUser` semantic mismatch should be flagged by CI.

Note: `package.json` and `tsconfig.json` are JSON and cannot start with comments.
