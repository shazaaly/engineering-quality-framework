# Scaffold Files

This folder contains copy-paste templates for target NestJS repositories.

- Why: make adoption fast and repeatable.
- How: copy these files into a governed repo, then run hooks + CI.
- Example: copy `.eslintrc.quality.js` and `quality-gate.yml` to start enforcement.

Note: JSON files cannot include comments by spec, so rationale lives in this README.
