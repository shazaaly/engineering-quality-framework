#!/usr/bin/env sh
# Why: Stop bad code before push.
# How: Runs lint-staged so only staged files are checked quickly.
# Example: `processUser` semantic mismatch gets blocked before commit.
npx lint-staged
